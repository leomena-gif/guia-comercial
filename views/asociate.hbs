<div class="gc-form-page">
  <div class="gc-form-header">
    <h2>Sumá tu comercio</h2>
    <p>Completá el formulario y llegá a más clientes. Tu solicitud será revisada antes de publicarse.</p>
  </div>

  <div class="gc-form-card">
    <form action="/asociate" method="POST">

      <div class="gc-field">
        <label class="gc-label">Nombre del profesional o comercio</label>
        <input type="text" class="gc-input" name="nombre" placeholder="Ej: Estudio Contable García" required>
      </div>

      <div class="gc-field">
        <label class="gc-label">Rubro</label>
        <input type="text" class="gc-input" name="rubro" placeholder="Ej: Contabilidad, Salud, Gastronomía…" required>
      </div>

      <div class="gc-field">
        <label class="gc-label">Descripción del servicio <span>(opcional)</span></label>
        <textarea class="gc-textarea" name="descripcion" placeholder="Contá brevemente qué ofrecés…"></textarea>
      </div>

      <div class="gc-field">
        <label class="gc-label">Teléfono</label>
        <input type="text" class="gc-input" name="telefono" placeholder="3512345678" required>
      </div>

      <div class="gc-field">
        <label class="gc-label">Email <span>(opcional)</span></label>
        <input type="email" class="gc-input" name="email" placeholder="tu@email.com">
      </div>

      <div class="gc-field">
        <label class="gc-label">Sitio web <span>(opcional)</span></label>
        <input type="url" class="gc-input" name="web" placeholder="https://tu-sitio.com">
      </div>

      <button type="submit" class="gc-btn gc-btn-primary gc-form-submit">
        <i class="bi bi-send"></i> Enviar solicitud
      </button>
    </form>

    {{#if message}}
    <div class="gc-alert gc-alert-success">
      <i class="bi bi-check-circle-fill"></i>
      {{message}}
    </div>
    {{/if}}
  </div>

  <p style="margin-top:20px;font-size:.8rem;color:var(--color-muted);text-align:center;">
    ¿Ya estás registrado? <a href="/contacto">Contactanos</a> para modificar tus datos.
  </p>
</div>
